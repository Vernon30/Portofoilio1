function toggleMenu() {
    const navbarLinks = document.querySelector(".navbar-links");
    navbarLinks.classList.toggle("open"); // Toggle the 'open' class to show/hide the navbar links
  }

function openLinkin() {
  const url = "https://www.example.com"; // The URL to navigate to
  window.location.href = url; // Redirects to the URL in the same tab
  }

function openGitHub() {
  const url = "https://www.example.com"; // The URL to navigate to
  window.location.href = url; // Redirects to the URL in the same tab
  }